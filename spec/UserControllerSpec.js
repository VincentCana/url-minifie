const userCtrl = require('app/controllers/UserController.js');

const methodPrefix = "=> ";
const messagePrefix = "-- ";

describe("UserController", () => {
    // Method UT
    describe(methodPrefix + "", () => {
        // Test case - duplicate this for multiple tests
        it(messagePrefix + "", next => {
        });
    });
});
